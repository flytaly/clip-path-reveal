{
    "name": "clippath",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "dev": "parcel src/index.html",
        "build": "parcel build src/index.html --public-url /clip-path-reveal/",
        "deploy": "rimraf dist && npm run build && npx gh-pages -d dist"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "gsap": "^3.11.4",
        "rimraf": "^3.0.2"
    }
}
